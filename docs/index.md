
## Architecture

| File | Summary |
|------|---------|
| [overview.md](architecture/overview.md) | High-level system architecture: frontend, backend, LLM providers, external services interaction |
| [admin-impersonation.md](architecture/admin-impersonation.md) | Admin user impersonation: session cookie `imp` field, impersonation endpoints, amber UI state |
| [admin-system-monitor.md](architecture/admin-system-monitor.md) | Admin-only `/admin/system-reports` "System Reports" operator dashboard (old `/admin/system-monitor` URL redirects; API prefix keeps the `system-monitor` name; deep links served by the `/admin/{rest:path}` SPA catch-all in quest.py); shell page with a left-hand section nav (`SECTIONS` in AdminSystemReportsPage.tsx) hosting four sections: "Latest Conversations" polls latest active conversations (sorted by `Conversation.last_message_at`) every 60s with visibility pause, in-flight dedupe, a live "Updated Xs ago" label, a manual RefreshCw button, a ⏰ routine badge + "Include Routines" filter (server-side `include_routines=false` + instant client pass); "Cost Analysis" is fetch-on-demand with a date-range preset dropdown (Last 7/30/90 days, Last 12 months, All time, custom start/end UTC dates) showing the top-30 most-expensive conversations ranked server-side by known cost (unpriced models rank by what CAN be priced, display total still nulls; deleted conversations still rank, owner resolved from call rows); "Users" is fetch-on-demand with the same range picker (shared ReportDateRange component) showing one row per user (whole roster, zero-activity rows kept, sorted by known in-range cost) with range-clipped non-routine active days (union of per-conversation user-message day sets), non-routine in-range conversation count, the estimated cost split into non-routine vs routine spend (each split independently nulls when a model in it is unpriced), a "By routine" itemization of the routine spend (`routine_costs`: per-routine name/project/cost rows ranked priciest-first with a "+N more" collapse, labels via `get_routine_labels()`), and per-model token aggregates across all the user's conversations via `get_usage_by_user()`; "Guides" is a fetch-on-demand deprecation tracker listing every guide in the system with its owner -- one row per `guides` table row (incl. empty auto-created defaults, with the count of routines still referencing the guide as an override via `list_all_guides()`) plus one row per project with non-empty Project Instructions (`projects.guide`, via `list_all_project_guides()`), content shipped as length only, client-side "Hide empty" toggle; all report handlers build their rows in `asyncio.to_thread` because the per-conversation chat_history.json reads would otherwise stall the event loop and the persistent WebSocket; the conversation sections share the ConversationUsageCell per-model token breakdown (provider-native rows + exact-fields tooltips; costs are OpenRouter's provider-reported per-call amounts where captured -- `llm_calls_openrouter.cost` -- else list-price estimates from db/llm_pricing.py with per-call >200K long-context tier grouping, every figure tagged `cost_source` reported/estimated/mixed and the FE drops the `~` prefix only for fully reported figures) (the Users section reuses the cell for its per-user aggregate) plus two columns: `latest_context_tokens` (input-side size of the latest top-level call, batched MAX(id) per provider table) and `active_days` (distinct UTC days with user messages from chat_history.json); `last_model` from `Conversation.model` is a proxy because per-message model isn't tracked |
| [auth.md](architecture/auth.md) | Sign-in methods: exactly one of Google OAuth or email/password is active, chosen by server_config.json `login_method` (`login_method()`/`is_password_login()` in auth/config.py; absent = "google" for pre-existing installs, the prod bootstrap wizard defaults new deployments to "password"); accounts are the same email-keyed `users` rows under both, so the one-way admin switch to Google (Settings > Sign-in, PUT /admin/sign-in/login-method, refused until the Google OAuth client is configured, written via update_server_config()) keeps every account, and reverting is a hand config edit; password routes in auth/password_login.py (`/auth/password/login|request-link|link-info|set|change`, all 404 `password_login_disabled` under Google sign-in, while the Google login routes refuse under password sign-in): stdlib scrypt hashes in `users.password_hash` (config/password_hashing.py, only the `password_fp` fingerprint rides on user dicts), passwords set only via one-time `password_tokens` links in the URL fragment (/set-password#token=..., SetPasswordScreen.tsx) -- 7-day admin invite/reset links (POST /admin/sign-in/password-links, optionally emailed, allow-lists addresses outside the admission policy), 1-hour self-service links when the optional core `smtp` credential service is configured (auth/mailer.py; doubles as sign-up), and bootstrap-wizard admin passwords handed over through `<data_dir>/pending_admin_passwords.json` and applied at startup; timing-equalized login with in-memory per-account/per-IP rate limits; password-issued session cookies carry the fingerprint (`pw`), so a password change signs out other sessions and a switch to Google ends all password sessions; plus OAuth flows, session cookies, credential handling, shared signed session-bound OAuth state cookies (`auth/oauth_state.py`: `mint_oauth_state`/`verify_oauth_state`/`clear_oauth_state`, timed itsdangerous payload `{csrf, uid, popup, ...extras}` -- Twitter's PKCE verifier lives there -- HttpOnly/Lax/`Secure`-when-session-is, 10-min TTL, constant-time nonce check, `uid` must match the session user, cleared on every callback outcome; the app login callback now enforces a state nonce minted with `/auth/login-url` and the `/auth/` page, where it previously accepted any `code`), two-tier Google OAuth (GOOGLE_SERVICE_SCOPES now includes the full cloud-platform scope for GCP; callback stores granted-not-requested scopes so a partial grant can't mask needs_reauth, requests the `userinfo.email` identity scope outside GOOGLE_SERVICE_SCOPES and FAILS CLOSED unless UserInfo proves the authorizing Google account is the session account -- non-200 or missing email never persists the token), per-service OAuth; all OAuth redirect_uris built via oauth_base_url() honoring the optional server_config.json `app_base_url` full public URL (env QUEST_APP_BASE_URL; also the base for app-generated absolute links like Slack attribution via get_app_base_url(), plain-text "Quest" when unset) then the hostname-only `oauth_hostname` override (raw-IP dev servers; e.g. dev.example.com via /etc/hosts); Google login callback always enforces is_login_allowed() -- allowed domain via allowed_login_domain() (QUEST_ALLOWED_LOGIN_DOMAIN local-only, then the server_config.json `allowed_login_domain` key, then hardcoded ALLOWED_DOMAIN) OR the optional server_config.json `allowed_login_emails` individual-address whitelist (personal deployments admitting e.g. @gmail.com accounts; both prompted by the prod bootstrap wizard, also enforced per-request by check_user_allowed() inside EVERY auth dependency via `require_user_allowed()` -- unchecked `auth/session.py` deps used by the script tool-call bridge, authed-get/post proxy, Gmail Simple and root `/api/*` endpoints included, so revoked users' still-valid cookies/API keys get 403 everywhere -- UI restriction note via login_restriction_description() never enumerates the whitelist); login callback also persists the Google `sub` claim into the unique nullable `users.google_sub` column (dev logins seed `dev-`-prefixed fakes; consumed by plugin integrations that resolve users by Google identity, with a lazy userinfo backfill) |
| [conversation-flags.md](architecture/conversation-flags.md) | Per-conversation opt-in behaviors set at conversation start via two web-only first-message paths merged into one set: a composer Flags popover (out-of-band `flags` field on send_message) and a magic `%%flags[name,...]` first line (Slack/scheduler first messages set none); registry in chat/conversation_flags.py (KNOWN_FLAGS, FLAG_LABELS, parse_flags_line, is_flag_enabled) hand-mirrored in FE constants/flags.ts (drift caveat); magic line's original message persisted/displayed verbatim, stripped copy sent to model + sidebar title; flags read from the row each turn and threaded into run_conversation_turn (also on wait-handle resume); post-first-message read-only "N flag(s) enabled" composer label hydrated from GET /conversations/{id}; flags: `nested_subagents`, `user_subagents` (gates proposing run_user_subagent, caller side only; additionally requires the server-global admin feature gate in feature-gates.md -- closed gate hides the flag from the popover and drops it via `filter_gated_flags()` at send) |
| [data-paths.md](architecture/data-paths.md) | Centralized data-directory path constants in config/paths.py with optional data_dir override; the ONLY id-to-path resolvers live on `ChatStorage` (`get_conversation_dir`/`get_project_dir`/`get_project_db_path`/`get_workspace_path` in chat/storage.py -- reject non-canonical ids and containment-check against CHATS_DIR/PROJECTS_DIR incl. planted symlinks, raising `InvalidStorageIdError`, a ValueError; no production code joins the roots with an id itself, migrations keep their historical joins) plus the separate user-aware guard for HTTP routes in chat/conversation_access.py (`require_owned_conversation`/`resolve_owned_workspace`/`require_owned_project`/`resolve_owned_project_dir`: DB ownership lookup + validated path, 404 on miss; used by file routes, conversation routes, project-table routes, Gmail URL-lookup routes and the Gmail draft route's workspace attachments) -- structural follow-up to security finding #279217 |
| [encryption-at-rest.md](architecture/encryption-at-rest.md) | Encryption at rest for every stored secret: one random 256-bit data key (AES-256-GCM, fresh nonce per value, `qenc1:<key_id>:<b64>` envelope with a per-column/file associated-data label) wrapped by an Argon2id password-derived key in `<data_dir>/encryption_key.json` (`config/encryption.py`, stdlib-only at import so `run.py` can use it; `python -m config.encryption init|check|rotate-password` CLI); password resolved at startup from `QUEST_ENCRYPTION_PASSWORD`, then `QUEST_ENCRYPTION_PASSWORD_FILE`, then the fixed public local-mode sentinel `LOCAL_DEV_PASSWORD` (refused in staging/prod on both create and unlock), then a confirmed TTY prompt, else a non-interactive startup aborts; `run.py` step 3 resolves the password before the build steps, exports it only for the alembic/seeder/uvicorn subprocesses and runs the `init` CLI (creates or verifies the key file, encrypts leftover plaintext credential store files), the lifespan unlocks it again fail-fast for manual uvicorn starts; ORM `EncryptedText`/`EncryptedJSON` TypeDecorators in db/encrypted_types.py keep the declared SQL types (autogenerate-quiet), tolerate legacy plaintext on read, and cover `users.api_key/google_oauth/google_services_oauth/airtable_token/ramp_oauth` + `user_service_credentials.secret/oauth_blob` (the Telegram session moved from the dropped `users.telegram_session` column into an `oauth_blob` row via migration `d7a1f3c9e2b4`); `users.api_key` stays encrypted (needed in plaintext for GET /me and the proxy preamble; sandbox containers now get ephemeral tokens instead) with lookups via the new SHA-256 `users.api_key_hash` column maintained by a `@validates` hook, and the Slack `get_user_by_slack_user_id` reverse lookup decrypts in Python instead of `json_extract`; service/inference credential store files are `{"encrypted": "qenc1:..."}` JSON so the stdlib-only bootstrap/run.py presence checks still work; single in-place migration `c4e8a2d17f63` (no intermediate release: sqlite3 online-backup `quest.db.pre-encryption-<ts>.bak` first when rows exist, idempotent by envelope prefix, downgrade decrypts + drops `api_key_hash` via native DROP COLUMN); NOT covered: `secret_key`, the Vertex service-account key file the Google SDK reads itself, legacy project-root credential files, `users.settings` |
| [expensive-resume.md](architecture/expensive-resume.md) | Expensive-resume warning for long-idle, long-context standalone conversations (project conversations never warn -- the card's alternatives don't apply there): rule registry in chat/expensive_resume.py (`EXPENSIVE_RESUME_RULES` dataclass tuple, exact model ids -- no prefixes, Vertex naming drifts -- + min context tokens + min idle seconds; first rule = the five Opus ids incl. Opus 5.5, 300K+ tokens, 2h+ idle), context size from the latest top-level `llm_calls_*` row via `get_latest_context_tokens()` (sub-agent rows excluded, pricing-tier context formula) behind a DB-free model/idle pre-check, verdict incl. an `estimated_resume_cost_usd` list-price figure (full cache re-write at the 5m rate via db/llm_pricing.py) shown in the card's first sentence; verdict surfaced as `expensive_resume` on GET /conversations/{id} and enforced by a `send_message` gate rejecting unacknowledged sends (`expensive_resume_unacknowledged`) unless the envelope carries `expensive_resume_acknowledged: true`; FE blocks the composer with a warning card (ExpensiveResumeWarning.tsx: Duplicate Workspace / Create Project from Chat / Continue anyway + TokenCostModal "Learn more"), ack state in conversationStore is in-memory only (continuing resets last_message_at so the rule stops matching) |
| [feature-gates.md](architecture/feature-gates.md) | Server-global admin on/off switches for optional features (all off by default), persisted in `data/feature_gates.json` via the stdlib-only `config/feature_gates.py` registry/store; features in `PER_USER_ACCESS_FEATURES` (currently `public_projects` and `guides`) additionally support per-user access -- an enabled gate can be open to all users or restricted to an `allowed_users` email list (case-insensitive; enforced everywhere via `is_feature_enabled_for_user()`, `enabled_features(user_email)` on GET /me is per-user, list preserved across off/on toggles, malformed list fails closed); admin-only GET/PUT `/admin/feature-gates` endpoints (PUT `allowed_users`: omitted = keep, null = all users) + desktop-only Settings > Features section (FeatureGatesSection.tsx, immediate-save toggles + a "Who has access" All-users/specific-users checkbox-roster editor fed by `GET /admin/users?include_self=true`); gates two features: cross-user subagents (`user_subagents` -- all-or-nothing, sits on top of the per-conversation flag: FE hides the flag from the composer popover via `enabled_features` on GET /me, `filter_gated_flags()` drops it at the send chokepoint, and the conversation loop + run_user_subagent handler reject proposals/approvals while closed) and public projects (`public_projects` -- a project-row gate, NOT a flag: closed-for-the-user = creation 400s + checkbox hidden, existing public projects vanish from GET /projects and 404 on by-id endpoints via `_get_visible_project()` without being deleted, and run_conversation_turn refuses new turns in their conversations; restoring access brings everything back); plus the legacy guides gate (`guides`, off by default so new installs never see guides while an existing install keeps them alive per-user during the skills migration: closed = every /guides route 403s `guides_disabled`, routine `guide_id` rejected on create/update (clear_guide allowed), run_conversation_turn ignores explicit guide_ids AND existing snapshots without deleting them, the custom_system_prompt->default-guide settings sync is skipped, FE hides the Settings Guides section + New Routine guide picker; the admin Guides report stays ungated) |
| [database.md](architecture/database.md) | SQLite via SQLAlchemy ORM with Alembic migrations (all per-user secret columns -- OAuth blobs, tokens, `users.api_key` with its plaintext `api_key_hash` lookup twin -- encrypted at rest via db/encrypted_types.py, see encryption-at-rest.md) for users (incl. the nullable email/password sign-in `password_hash` plus the one-time link table `password_tokens`, migration `e5b2c8f14a37`), memories, guides, projects (incl. the immutable creation-time `public` boolean -- see public-projects.md), routines (with per-routine auto-load skills via `routine_skill_autoloads`), conversations (with origin, last_message_seq high-water cache, auto_title sidebar-title cache, and a nullable `flags` JSON column for per-conversation opt-in behaviors set at start; per-user `settings` JSON carries `slack_default_model`, the separate web/composer `default_model`, and the Quest-manageable Gmail label list `gmail_labels`), slack_conversations, tool_wait_handles (slack_reply + action_request kinds, both always blocking), user_service_credentials (per-user plugin-service credentials -- one row per (user, service), secret written by the generic /auth/service-key/{service} routes or oauth_blob written by plugin oauth routers (migrations `a7c3e91b52d8`/`e2f8a4c61b93`/`f3a9c5d81b42` moved users.github_oauth/users.twitter_oauth/users.slack_oauth into github/twitter/slack rows), attached to user dicts as `service_credentials` and cleared by logout-and-disconnect / CASCADE on account delete), and the append-only per-provider raw token analytics tables `llm_calls_gemini` / `llm_calls_anthropic` / `llm_calls_openrouter` (each provider's native usage fields verbatim -- incl. the Anthropic cache-creation 5m/1h TTL split and the OpenRouter-reported `cost` / `upstream_inference_cost` / `is_byok` accounting columns added by migration `f7d2a9c41e58`, preferred over the list-price estimate by the read queries -- plus shared dimension columns and a `raw_usage` JSON catch-all; no normalized in/out/cached triple, all interpretation in db/llm_call_store.py read queries + the static price table in db/llm_pricing.py; migration `05cdf20e3f2f` backfilled `gemini_api_calls` history, approximating legacy Anthropic merged cache as cache_read) |
| [frontend.md](architecture/frontend.md) | React + Vite + TypeScript frontend (chat markdown renders LaTeX math via remark-math + rehype-katex with self-hosted KaTeX fonts and a `remarkMathCurrencyGuard` post-pass keeping "$5 and $10" as text; design tokens on `:root` in index.css -- `--surface` chat ground, `--surface-raised` nav sidebar/composer/right-panel cards, `--text*`, `--hover`, `--border`, the accent family `--accent`/`--accent-rgb`/`--accent-hover`/`--accent-contrast`/`--accent-text` with alpha tints derived via `rgb(var(--accent-rgb) / a)`, `--shadow-card` -- dark on `:root`, light overrides in the prefers-color-scheme block; component stylesheets reference the accent only through tokens; user-selectable colour THEMES swap the accent family + the two grounds for both schemes at once via `<html data-color-theme>` -- `prototype` (= the `:root` defaults, indigo on neutral greys), `electric-blue` (Electric Capital cyan/grey ramp) `alloy` (forest green + cream) and `recall` (sage-mint on near-black) in src/themes.css, registry in utils/colorTheme.ts, persisted in `users.settings.color_theme` via PUT /settings (`COLOR_THEME_CHOICES` allow-list in chat/routes/user.py, hand-mirrored) + `color_theme` on GET /me, cached in localStorage `quest_color_theme` and applied pre-paint by the index.html boot script, picked in Settings > Appearance > Theme (AppearanceSection.tsx swatch tiles); Settings > Appearance light/dark/auto selector (AppearanceSection.tsx) persisted in `users.settings.theme` via PUT /settings + `theme` on GET /me, cached in localStorage `quest_theme` and applied to `<html data-theme>` pre-paint by an inline index.html script, with the build-time PostCSS `themeOverridePlugin.ts` rewriting every `@media (prefers-color-scheme)` block into a `:where(:root:not([data-theme=other]))`-scoped auto copy plus a `:where(:root[data-theme=scheme])`-scoped forced copy at zero extra specificity, so stylesheets keep using plain media queries): URL-based routing, API client, UI components, inline workspace markdown images in assistant replies (`![alt](chart.png)` resolved by the shared `img` renderer `MarkdownImage` via `MarkdownWorkspaceContext` to the cookie-authed files/download endpoint, click-to-enlarge via FileViewerModal, per-URL broken-ref fallback chip; external/absolute srcs are NEVER auto-fetched -- they degrade to click-through links to block image-based exfiltration; system prompt + `system:workspace` skill teach the model the convention), a single shared `<Composer>` footer (one floating rounded bubble with an icon-only controls row on every viewport -- desktop keeps the row always visible, phone widths collapse to an input pill until focused/holding a draft and overlay the messages area; extracted from ChatPanel, reused by the root `HomeComposer` home screen at `/` that greets "What can I help you with, <first name>?" over a bottom-pinned branding footer ("Remember AI can make mistakes, but <appName> keeps you in control." / "Made with <heart> by Electric Capital" -- the heart is the monochrome currentColor `HeartBoltMorph` SVG, one closed path of eight cubics SMIL-animated between a heart and the tall vertical-edged Electric lightning bolt, box rendered taller than the line and centred on the text via negative vertical margins on a 14s loop: 5s hold / 2s morph / 5s hold / 2s morph, static heart under prefers-reduced-motion; the same glyph replaces the former pink ♥ in Settings > About; About shows the running release -- version, release date, commits-past-tag note -- fetched from GET /app/api/version), is the landing target of EVERY "New Chat" button (sidebar brand bar, Conversations `+` at top level and inside a project, phone top bar -- none pre-create an empty conversation; the row is created as a response to the first message, brand-bar and project `+` clicks while drilled stay drilled so the chat lands in the project), and carries the chosen model/skills/flags into the first send via `ConversationContext.pendingFirstMessage` (when the Sidebar is drilled into a project -- context `drilledProjectId`, e.g. right after creating an empty project -- the first send creates the chat inside that project with a "New chat in <name>" hint instead of a standalone one); HomeComposer passes the composer's `skipSendLocks` prop so the first-send provider lock is written on the real conversation id by ChatPanel's pendingFirstMessage effect, never on the `HOME_DRAFT_KEY` draft key (constants/drafts.ts) -- a persisted draft-key lock would filter the home model dropdown to one provider forever, and ConversationContext strips legacy draft-key entries from the persisted lock map on init; a paperclip "Attach" button queues generic files (200 MB/file, 20 max) uploaded to the workspace via the existing POST /files/upload before the first send -- distinct from the clipboard-image path -- with names threaded as `attached_filenames`; the new-conversation default model is server-side per-user `users.settings.default_model` re-fetched fresh from GET /me on every new-composer mount, no localStorage, persisted via PUT /settings only on first send, fallback prefers Opus 4.8 but degrades to the first credentialed model per available_models from GET /app/api/config -- e.g. a Gemini model on a Gemini-only local instance; the model list itself is a runtime catalog in constants/models.ts fed by `setModelCatalog()` from the `models` field of GET /app/api/config -- Vertex registry + admin-configured OpenRouter instance models under `<instance>:<wire_id>` ids, instance label folded into the display name -- with a Vertex-only static fallback pre-fetch and `getKnownModels()`/`getSelectableModels()`/`getModelInfo()` lookups), right panel (file browser + project tables rendered as floating `.right-panel-card` units in a transparent gutter; while the home composer is open inside a drilled project -- URL `/`, no conversation yet -- RightPanel falls back to the context `drilledProjectId` and borrows any existing conversation of that project as the FileBrowser workspace handle, since all project conversations share one workspace directory, so the project's files and tables show instead of the standalone empty state -- raised surface, rounded, shadowed -- with hover-revealed resize pills: horizontally resizable via a left-edge col-resize handle with width persisted browser-wide in localStorage under `quest_right_panel_width` -- 280px default/min, max Math.min(700, innerWidth-600), read on mount, re-clamped on window resize; the gap between the two cards is the row-resize split handle; incl. in-modal PDF preview via PdfViewer.tsx -- direct pdfjs-dist with thumbnail rail, lazy canvas rendering/eviction, 100 MB cap, all pdf.js assets self-hosted under dist/assets/ via ?url worker import + vite-plugin-static-copy for the no-egress deployment; in-modal markdown HTML preview rendered by default via react-markdown reusing the chat renderer's markdownComponents, with a "Show rendered" header toggle for raw source; and in-modal CSV preview rendered by default as a sortable table reusing project-tables styling via the dependency-free RFC-4180 parser in utils/csv.ts, with a "Show raw" header toggle and 5000-row cap, sorted client-side since CSV has no backend query layer), state management, and a paged sidebar conversation list (top-level list fetches 30 rows/page with server-side filters; older pages auto-load on scroll via an IntersectionObserver on the still-clickable "Load more" keyset-cursor pager row; background refreshes re-fetch only the loaded window via `topFiltersRef`/`loadedCountRef` so WS closures never use stale filter state; activity-driven re-sorts FLIP-animate rows to their new slot via hooks/useFlipListAnimation.ts); consumes events from the persistent multiplexed WebSocket via PersistentWebSocket and the WebSocketManager shim; sidebar top is a brand bar (logo + app name, New Chat icon button, search icon button, requests inbox icon button with open-count badge -- replaces the old Requests/Search label rows); the brand-bar New Chat pencil opens the home composer targeting the drilled project when the sidebar is drilled into one (same path as the drill-down "+"), a standalone chat otherwise; ChatPanel opens with a Claude-style conversation title unit (ConversationHeader.tsx: title + chevron dropdown with Rename inline input / Archive-Unarchive, R-A key hints, no delete, state hydrated from the `custom_name`/`archived`/`title` fields on GET /conversations/{id}) over spaced bold-header sections with no hairline dividers and pill-shaped rows; chat messages have no role headers -- timestamp (+ model name) sits in a hover-only footer under each bubble, the messages list is capped at 720px with 2rem desktop gutters, inline code is bold/tinted with no background, collapsed tool-call rows are low-contrast; the phone shell's top bar has a New Chat button beside the menu button and shows folder icon + project name while drilled into a project |
| [gemini-api.md](architecture/gemini-api.md) | Conversation loop, streaming, system prompt with system-skills enumeration, memory read tools, workspace file tools (incl. edit_workspace_file exact string replacement gated by a read-before-edit check tracked in a per-conversation workspace_reads.json sidecar that survives restarts and covers sub-agents; get_workspace_file and load_gmail_attachment run a pre-flight size check against the per-backend attachment caps in chat/llm/file_limits.py -- resolved for the actually-called model incl. sub-agents, unknown models get the smallest cap -- rejecting oversized files with a structured error + actionable suggestion instead of a history-poisoning provider 413, covering the large-text inline fallback too), blocking create_action_request (memory writes ride on request_type="create_memory"; skill create/edit ride on create_skill/edit_skill), wait_for_handles and three suspend sentinels (wait_handles / slack_reply / action_request), skill loading (list_skills/search_skills/load_skills) plus read-only skill inspection (list_my_skills/get_skill) and project-routine inspection (list_routines) in BASE_TOOLS, Slack read tools as dedicated tool_call tools registered by the in-tree Slack plugin (list_slack_teams/list_slack_conversations/get_slack_conversation_history/get_slack_conversation_replies/search_slack_messages/get_slack_user_info/list_slack_users wrapping the plugins/slack/upstream.py endpoint functions -- team_id resolution, 50-cap notices merged into the JSON result, 429 retries stay server-side -- plus the no-approval send_slack_dm_to_self self-DM send tool (bot-delivered, optional workspace-file attachments via Slack's external upload flow, max 10 files / 50 MB each; replaces curl-to-dm-self entirely -- the /api/slack-simple/dm-self route is retired, scripts use the tool-call bridge -- and it is the one connector tool in PUBLIC_TOOL_CALL_ALLOWLIST -- see public-projects.md); no Slack HTTP routes remain, /api/slack + /api/slack-simple blocked in route dispatch, sandbox scripts reach allow-listed tools via the POST /api/tool-call bridge in chat/gemini_api/script_tool_call.py -- SCRIPT_TOOL_CALL_ALLOWLIST = Slack reads + send_slack_dm_to_self + find_slack_channel + Gmail Simple/Quest-label tools + memory reads, same {tool_name, arguments} shape as tool_call, conversation_id=None degradation, /api/tool-call itself proxy-blocked), authed API requests (authed_get plus the POST-capable sibling authed_post for read-shaped POST verbs like GCP organizations:search/entries:list, gated by an independent verb-scoped allowed_post_endpoints allow-list; both share credential injection, 401 retry, size gate, and the optional output_file that writes response bodies non-destructively under the hidden `.responses/` workspace subdir, bypassing the size gate), nested 2nd-level sub-agents (the `nested_subagents` flag gives 1st-level sub-agents an `agent_task_nested` spawner, tier SUB_AGENT_TOOLS_NESTED, restricted to NESTED_SUB_AGENT_ALLOWED_MODELS = Haiku/Flash-Lite, no 3rd level, per-layer prompt copy), and durable error surfacing (run-fatal exceptions append a persisted `error` message -- in FLUSH_EVENT_TYPES -- with partial streamed text flushed first; GeminiProvider raises GeminiStreamAbnormalTermination on abnormal finish_reason instead of a silent empty turn, MAX_TOKENS delivers already-streamed output; _dispatch_tool_call converts handler exceptions into structured error results so one failing tool never kills the run); invoked by chat/realtime/socket.py:_run_send_message |
| [realtime.md](architecture/realtime.md) | Persistent multiplexed WebSocket (WS /app/api/stream): subscribe/catchup/resync with monotonic seq and 5-min TTL refreshed every 3 min by the FE (no eager unsubscribe on tab switch; subscription_expired envelope on TTL lapse), send_message/stop control plane with optional pasted-image attachments and an optional `attached_filenames` list (generic composer files pre-uploaded via POST /files/upload, surfaced only in the triggering turn's message_metadata), attachments_unsupported_during_resume / user_not_found / expensive_resume_unacknowledged reject reasons, a sender-only `send_message_accepted {seq, client_send_id?}` delivery receipt sent right after the user row is appended (the FE stamps every optimistic bubble with a `client_send_id`, fails the send when the socket is closed or no receipt arrives within 15s, and renders the bubble dimmed with Retry / Discard instead of leaving it hanging until reload) (the user is re-read fresh from the DB each turn so credential/connection-gated decisions never use the stale connect-time snapshot; the last requires the `expensive_resume_acknowledged: true` envelope field -- see expensive-resume.md), and an optional out-of-band `flags` field (composer Flags popover, first-message-only), every `subscribed` ack carries `run_active` and the FE reconciles its streaming/stop state against it (`WebSocketManager.syncRunState`: drains a stale spinner when the transient `send_message_finished` was lost across a socket drop, enters the stop state on reload / second tab mid-run), durable message_appended events, transient text_delta and sub-agent events (with nested_agent_id/nested_agent_name/nested_agent_model/nested_agent_status/nested_parent_id fields for 2nd-level nested sub-agents), per-user globals (request_count_changed, conversation_list_changed, file_list_changed, wait_handle_resolved), and a send_message_finished `error` flag (run raised; durable error bubble arrives via message_appended, FE skips the success desktop notification); replaces per-turn WS and 30s badge polling |
| [guides.md](architecture/guides.md) | Named system prompt presets with per-conversation snapshot mechanism; deprecated in favor of skills and the WHOLE feature sits behind the off-by-default per-user-capable `guides` admin feature gate (feature-gates.md: closed = 403 `guides_disabled` on every /guides route incl. convert-to-skill, routine guide overrides rejected, no explicit or snapshotted guide applied by run_conversation_turn, settings sync skipped, Settings section + routine picker hidden; nothing deleted, reopening restores) -- creation disabled (POST /guides returns 410 guides_deprecated), composer guide selector and per-conversation guide selection/locks removed, NO default-guide fallback for new conversations (run_conversation_turn resolves a guide only from an existing snapshot or an explicit guide_id -- routine guide overrides, the sole remaining source, via pendingRoutineMessage/scheduler), Settings Guides section kept for the transition (notice linking to Skills, per-guide POST /guides/{id}/convert-to-skill: private skill from the guide's name+content with (converted) suffix retry on collision, default guide's skill gets user auto-load, guide deleted afterwards incl. the default), and no default-guide auto-recreation anywhere; unrelated: the project `guide` field is now labeled "Project Instructions" in the UI (see projects.md) |
| [llm-providers.md](architecture/llm-providers.md) | Multi-provider abstraction layer supporting OpenRouter (openai SDK against the OpenAI-compatible chat-completions API; one `OpenRouterProvider(instance_id)` per admin-configured provider instance, each reading its own key from the inference-credential store; OpenAI-format sessions with system prompt prepended per call, streamed tool-call fragment accumulation, the `usage: {include: true}` accounting opt-in so the final usage chunk's `cost` / `upstream_inference_cost` / `is_byok` land in raw_usage, no file attachments, no compaction; models are NOT registry entries but per-instance admin-chosen lists with `<instance_id>:<wire_id>` qualified ids resolved by `resolve_model()`/`ModelSpec` -- `MODEL_REGISTRY` is Vertex-only, see inference-providers.md), Google Gemini (google-genai in Vertex AI mode only -- the genapi developer-endpoint transport was removed because thought signatures don't cross the Vertex/AI Studio border; images/PDFs inlined as Part.from_bytes; `gemini-3.1-flash-lite-preview` maps to the `gemini-3.1-flash-lite` Vertex id) and Anthropic Claude (Vertex AI, with registry-gated adaptive thinking: `thinking_effort` in MODEL_REGISTRY -- currently "medium" on Opus 4.8 and Opus 5.5, the latter because Opus 5.5 rejects thinking-off requests -- adds `thinking: adaptive` + `output_config.effort` to every request, thinking/redacted_thinking blocks preserved in session history for replay with all-thinking-turn and unsigned-partial-block guards, display stays "omitted" so no UI change; and registry-gated refusal fallbacks: `refusal_fallback_models` in MODEL_REGISTRY -- currently Opus 5 -> Opus 4.8 and Opus 5.5 -> Opus 4.8 -- registers the SDK's client-side `BetaRefusalFallbackMiddleware` (Vertex has no server-side `fallbacks` param; send path on `client.beta.messages.stream`; built with `betas=REFUSAL_FALLBACK_BETAS` = none because Vertex 400s on the SDK-default `fallback-credit-2026-07-01` beta on every request, so no credit-token repricing and only pre-output refusals chain) so safety-classifier refusals (`stop_reason: "refusal"`, e.g. benign cyber work on Opus 5) retry on the fallback chain with mid-stream splice behind a `fallback` seam block -- the streaming loop drops pre-boundary thinking, never persists the seam, records `{from_model, to_model, category}` into raw_usage, yields a `model_fallback` StreamEvent that `_stream_turn` persists as a user-visible `model_fallback` transcript marker row (in FLUSH_EVENT_TYPES; FE renders a violet "<from> declined ... answered by <to>" system notice via Message.tsx), and a per-session `BetaFallbackState` pins later turns of the run to the accepting model; a refusal surviving the whole chain raises `AnthropicStreamRefusal` (durable error bubble with category) instead of a silent empty turn); `get_backend_for_model()` resolves the cost-analytics backend label, and `UsageStats.raw_usage` carries each provider's lossless native token fields (Gemini thoughts/tool/total; Anthropic cache read/creation split incl. the 5m/1h cache-creation TTL detail) alongside the coalesced summary, feeding the per-provider `llm_calls_*` raw tables via db/llm_call_store.py; per-backend model-attachment size caps centralized in chat/llm/file_limits.py (Anthropic-Vertex ~15.75 MB effective from the 30 MB payload ceiling, Gemini-Vertex 7 MB inline, images 5 MB; never-raising get_attach_limit_for_model resolver) with provider upload_file caps as defense-in-depth behind the tool-handler pre-flight checks |
| [inference-providers.md](architecture/inference-providers.md) | Admin-only Settings > Inference Providers panel over two kinds of provider configuration: Vertex AI (single, credentials display-only -- detected `GOOGLE_APPLICATION_CREDENTIALS`/gcloud ADC + effective per-family project/region with source layer -- fixed `MODEL_REGISTRY` catalog with per-model enable checkboxes stored as `vertex.disabled_models`) and zero-or-more **provider instances** (`config/inference_providers.py` `INSTANCE_KINDS`, today only `openrouter`: each an OpenRouter configuration with admin label, its own key in `inference_credentials/<instance_id>.json` (encrypted at rest) and an admin-picked model list chosen from a cached `openrouter.ai/api/v1/models` catalog typeahead (`chat/llm/openrouter_catalog.py`, 24h cache, custom ids allowed) with name/context/output/pricing snapshotted per model into `data/inference_providers.json`; "+ Add OpenRouter configuration" footer button); instance models are identified everywhere by qualified ids `<instance_id>:<wire_id>` (`split_model_id`; legacy single key = instance `openrouter`, migration `b8e4d2a7c1f5` prefixed stored bare ids, credential files without a config entry are synthesized into instances so dev-config pre-baking still works); `resolve_model()`/`ModelSpec` in chat/llm/config.py is the single lookup path, provider singletons keyed `(provider, instance_id)`; admin API = GET list `{vertex, instances, kinds}`, PUT vertex disabled set, POST/PUT/DELETE instances (empty key keeps, full-replacement `models` list, delete removes key file + verdicts), GET openrouter/catalog, POST test-model (disabled -> 400); every model row shows the wire id string used in API calls with a liveness dot for the server-global `ModelHealthStore` verdict (in-memory + data/model_health.json; startup sweep over the enabled+credentialed `get_configured_models()` list at concurrency 4 -- disabled models are never checked; `get_available_models()` = configured minus explicit-failing verdicts filters the picker, `GET /app/api/config` also returns the `models` catalog the frontend builds pickers from), each check a minimal live inference call (`check_model_access()` per provider, 45s timeout) with the extracted provider error inline and Recheck buttons that update the store |
| [model-selection.md](architecture/model-selection.md) | Admin-only Settings > Model Selection table (one row per enabled, non-deprecated model, Vertex + instance models) layering presentation/usage settings over the provider config: a unique top-level composer-menu `slot` (1..5, slot dropdown swaps occupants) + free-text `descriptor` shown above the model name at the top level (replaces the hardcoded `RECOMMENDED_MODELS`); while the `public_projects` gate is on for anyone (`public_mode_enabled()`) the table additionally shows a separate `public_slot` for the public-project composer menu plus `allow_private` / `allow_public` "Allowed" checkboxes (private = everything outside a public project incl. routines/Slack/subagent/inference API runs; public = public-project conversations; a disallowed visibility's slot is cleared) and TWO menu previews (private + public) above the table, while with the gate off only Model/Slot/Descriptor and one side preview show and the stored public settings are kept but ignored (`is_model_allowed()` allows everything, catalog masks the flags); the allow flags -- unlike the Inference Providers enable checkbox -- are enforced per turn (`is_model_allowed()` in `run_conversation_turn` raises a durable error) as well as at selection (Composer filters `getSelectableModels(visibility)` by its `isPublicProject` prop, now also passed by HomeComposer from the drilled project, disables Send on a disallowed selection, ModelSelector renders `getTopLevelModels(visibility)` and suffixes "(not allowed here)"); stdlib-only store `config/model_selection.py` -> `data/model_selection.json` (absent file = historical Smart/Faster/Fastest defaults in both menus, saved file = whole truth, malformed = empty), fields delivered on every `models` entry of GET /app/api/config via `public_model_catalog()`, admin GET/PUT `/admin/model-selection` (full replacement, `public_mode_enabled` + per-row `available`/`unavailable_reason`, 400 on unknown model / bad slot / duplicate slot within a menu / long descriptor), FE section ModelSelectionSection.tsx with a draft + Save/Discard, `refreshModelCatalog()` on the context re-fetches the catalog after a save |
| [logging.md](architecture/logging.md) | Unified logging config with colored level labels, timestamps, process IDs, and module names |
| [oauth-popup.md](architecture/oauth-popup.md) | OAuth popup window architecture using postMessage for connector buttons in Settings |
| [action-requests.md](architecture/action-requests.md) | Agent-proposed write operations to external services (Slack/Telegram/Twitter messages, calendar invites, upload_to_drive multi-file raw-bytes workspace-file uploads to Google Drive (`files` list, up to 10/request, one destination folder, legacy single-`path` shape still accepted) with optional on-the-fly destination-folder creation, create_drive_folder Drive folder creation, edit_google_spreadsheet Google Sheets cell-range writes (current_values read-before-write verification at proposal + approve time; spreadsheet-style diff-table card via the structured `spreadsheet_diff` preview field rendered by SpreadsheetDiffPreview.tsx), reset_gcp_instance GCP VM hard resets (see gcp-api.md), memory saves via create_memory, plus plugin-registered types) plus internal-store routine writes (create_routine/edit_routine: project-scoped name/prompt/model/schedule/skill-autoload create + changes with same-turn precard validation, a prompt `content_diff` card preview on edits, and a proposal-time updated_at TOCTOU token -- see routines.md) and skill writes (create_skill/edit_skill: user-or-project target, visibility/email-sharing with auto-clear-on-leave-shared, project_autoload toggle, dual name-collision guards via skill_precard.py pre-card check + execute-time TOCTOU guard, system:* ids rejected; edit_skill content edits are an old_string/new_string search-and-replace gated on the conversation having loaded/read the skill via the skill_reads.json sidecar, resolved against current content at pre-card AND approve time, with a server-computed content_diff line diff rendered as a collapsible hunk snippet by SkillContentDiffPreview.tsx); create_action_request tool blocks the agent loop via SuspendForActionRequest until the user clicks Approve/Revise/Stop (revise sends feedback back to the model on resume; Stop -- replacing the old Deny -- marks the row + handle `stopped`, stops the conversation's other open cards, unlocks the composer and kicks NO resume: the dangling tool_use is closed only by the user's next message, with `verdict: "stopped"` + an explanatory note and that message in the same turn; subagent_return cards keep a hard Deny); several create_action_request calls in ONE parallel tool-call batch each open their own card + wait handle (the dispatch loop catches the sentinel per call and keeps dispatching the batch, regular sibling tools run and persist their tool_result), the composer stays locked until every card is resolved, and the headless resume is held by `_unresolved_blocking_handle_ids()` until no dangling card is pending so the model gets all verdicts at once; two-stage same-turn validation -- local `validate_params` rejects unknown keys / shape, then handlers with an upstream dry-run POST to `/validate/` for FK/enum/decimal/date rejections; per-handler preview enrichment via the `enrich_params_for_preview` hook (plugin handlers) alongside the legacy core ladder in turn_tools.py, both surfaced as `Invalid parameters` with no card (email-ownership conflicts are NOT dry-run-covered and 400 only at Approve); suspended conversation's FE composer is locked across tabs/reloads while the linked wait handle is pending; disabled in Slack-driven runs; per-backend specs in system skills; collapsed/resolved cards are server-rendered too -- each handler exposes resolved_label (Created/Saved/Uploaded/... shown after execution) + summary_snippet(params) (80-char params summary), carried on the durable action_request message and GET /action-requests/{id}, so the FE has no per-type rendering chain |
| [project-db.md](architecture/project-db.md) | Per-project SQLite database via project_db_query tool with row/size limits, ATTACH blocking enforced by a per-connection SQLite authorizer (denies SQLITE_ATTACH at compile time; the textual prefix check is only a fast path), and sortable table browser UI |
| [projects.md](architecture/projects.md) | Groups related conversations with shared workspace, optional project instructions (the `projects.guide` text field, UI label "Project Instructions" -- unrelated to deprecated guides), and skills auto-load; a standalone non-Slack conversation can be converted into a new project from its sidebar dropdown ("Create Project from Chat" -> `ConvertToProjectModal`), moving its workspace files into the project workspace and making it the project's first conversation; projects can be created as public (immutable `projects.public` mode, see public-projects.md) |
| [public-projects.md](architecture/public-projects.md) | Public projects: immutable creation-time mode (`projects.public`) giving conversations an internet-enabled sandbox while cutting off ALL internal resources -- no skills/memories/connectors/action requests/sub-agents/authed APIs (sole connector exception: the outbound-only `send_slack_dm_to_self` self-DM tool, allowlisted because its fixed recipient is the user themselves and it reads nothing beyond named workspace files); enforced at three layers (PUBLIC_TOOLS tier, dispatch-time PUBLIC_TOOL_CALL_ALLOWLIST hard-reject in `_dispatch_tool_call_inner` + `_PUBLIC_BLOCKED_LOOP_TOOLS` loop rejects, and the public sandbox profile); dedicated no-API-key system prompt (`get_public_project_system_prompt`); SEPARATE podman image `quest-script-runner-public-<mode>` (own Dockerfile + entrypoint: internet egress, DNS allowed to 10.0.2.3, iptables REJECT for RFC1918/link-local/CGNAT incl. cloud metadata, NO QUEST_API_KEY/PORT injected) built beside the restricted image by run.py; no routines, no project skills (API 400s + skill action-request guards), no from-conversation creation; whole feature behind the server-global `public_projects` feature gate (off by default, per-user capable: admin can open it to all users or an `allowed_users` email list, every check per-user via `is_feature_enabled_for_user()`; closed-for-the-user = creation 400 `public_projects_disabled`, existing public projects hidden -- list-filtered + 404 via `_get_visible_project()`, undeletable while hidden, run_conversation_turn refuses turns -- all restored when access returns); FE: NewProjectModal checkbox (hidden while gate closed), sidebar globe badge, hidden Routines/Skills sections, and an `isPublicProject` prop on the shared `<Composer>` driving a persistent don't-share-sensitive-info warning banner above the input (internet sandbox may send content to third-party sites) plus the hidden Skill button |
| [plugins.md](architecture/plugins.md) | Filesystem-discovered integration plugins (plugins/*/plugin.py -> get_plugin() -> QuestPlugin; extra search roots via the os.pathsep-separated QUEST_PLUGIN_PATH env var, scanned after the in-tree dir so in-tree ids win duplicates): loader validation (unique id, <id>_-prefixed action types/tools with the unprefixed_action_types/unprefixed_tools grandfather lists, system:<id> skills, script allowlist subset, oauth-kind rules) + skip-and-log resilience; manifest fans out into system skills, action-request handlers, tool_call tools (`PluginTool.mutating` flags approval-free external writes refused in inference API runs), authed_get services, script bridge, admin credential card (credential_schema/is_configured/credential_validate), and per-user connections (UserConnectionSpec: api_key kind with key_placeholder over the generic key routes, or oauth kind supplying an oauth_router confined to /auth/<id> and mounted by quest.py's mount_plugin_oauth_routers(), token JSON in user_service_credentials.oauth_blob, optional needs_reauth hook for the connector re-auth badge), plus an optional post_load zero-arg startup hook run by load_plugins() right after registration (plugin-owned one-time work, e.g. a legacy credential-store migration; failures logged, plugin stays loaded; not invoked by test fixtures); no plugin /api/* HTTP routes/FE code/DB columns; combined server-configured AND user-connected gating; four in-tree plugins (proprietary plugins can load from external repos via QUEST_PLUGIN_PATH): GitHub (plugins/github, id github, oauth kind), Microsoft 365 (plugins/m365, id m365, oauth kind with expiring/refreshing tokens), Slack (plugins/slack, id slack, oauth kind with non-expiring tokens, both grandfather lists -- unprefixed_action_types for send_slack_message/send_slack_dm and unprefixed_tools for its nine infix-named tools -- plus the core-owned _PUBLIC_ALLOWLIST_MIGRATED_TOOLS exemption keeping send_slack_dm_to_self public-project allowlisted; the Slack-driven Socket Mode runtime stays core), and Twitter/X (plugins/twitter, id twitter, oauth kind with expiring/rotating tokens + PKCE and the grandfathered send_twitter_dm unprefixed action type); plugin-specific tests live in the plugin's own tests/ dir (plugins/<id>/tests in tree, <root>/<id>/tests out of tree -- pytest testpaths covers plugins/, the repo-root conftest.py auto-collects QUEST_PLUGIN_PATH roots' suites on a bare run, fixtures via tests/plugin_support.py plugin_fixture() + config.plugins.unregister_plugin()) |
| [route-dispatch.md](architecture/route-dispatch.md) | Internal route dispatch for tool calls with conversation_id auto-injection and blocked proxy paths |
| [run-modes.md](architecture/run-modes.md) | Three run modes (`local`/`staging`/`prod`, legacy `dev` aliases to local) resolved centrally in config/environment.py with semantic capability flags (dev login, domain enforcement, per-mode cookie name + staging-only Secure, container image suffix); local mode boots with zero credentials (incl. the credential-encryption password: the fixed public `LOCAL_DEV_PASSWORD` sentinel from config/encryption.py, refused in staging/prod where the password comes from `QUEST_ENCRYPTION_PASSWORD`/`_FILE` or a TTY prompt -- see encryption-at-rest.md) -- throwaway per-run seeded data dir under data/local-runs (QUEST_DATA_DIR override, `--keep-data` to reuse), shared parent-directory dev-config.json pre-baked at startup (`vertex_service_account` -> ADC env, `service_credentials` -> per-service store with existing store files winning, `inference_credentials` -> inference-credential store (e.g. the OpenRouter key) with existing store files winning, `oauth_hostname` -> QUEST_OAUTH_HOSTNAME, `allowed_login_domain` -> QUEST_ALLOWED_LOGIN_DOMAIN; legacy bare vertex-service-account.json still honored), canned-account sign-in picker via GET /auth/dev-accounts + POST /auth/dev-login, `available_models` on GET /app/api/config (config-presence per backend minus health-store failures), plugin services hidden unless enabled/configured in the per-service store (structured not-configured errors otherwise, legacy locations migrated by plugin post_load hooks, else off by default), non-fatal Podman setup; prod mode runs the scripts/bootstrap_prod.py first-run wizard when admin_emails or google_oauth credentials are missing (non-interactive unconfigured startups abort unless QUEST_SKIP_BOOTSTRAP=1; `--bootstrap` re-runs it), and staging/prod export a bootstrap-written `<data_dir>/vertex-service-account.json` as ADC |
| [routines.md](architecture/routines.md) | Canned prompts attached to projects that run in one click with optional guide override, model, and per-routine auto-load skills (merged with user and project auto-loads on every invocation); routine conversations are named after their routine at creation (`custom_name` set by `ChatStorage.create_project_conversation()` for both one-click and scheduled runs) and their system prompt (`get_system_prompt(is_routine=True)`) drops the first-reply `set_conversation_name` naming instruction + tool; routine + schedule edits guarded by `updated_at` optimistic-concurrency token with a stacked conflict modal on 409 stale_update; agent tools: read-only `list_routines` (BASE_TOOLS, project-scoped) + the `create_routine`/`edit_routine` action requests (name/full-replacement prompt -- edit cards show a `content_diff` line diff via the shared SkillContentDiffPreview -- /model/full-replacement schedule/skill auto-loads, same-turn `routine_precard_check` + proposal-time token TOCTOU close, a model-less create gets a precard-injected default -- Sonnet 5 then Gemini 3.7 Flash by availability, card row marked "(default)" via `model_defaulted` -- guide override NOT settable/editable, no agent-side delete), spec'd in the `requires_project` `system:routines` skill; RoutineSettingsModal has no guide selection widget anymore -- a leftover guide shows read-only with a Clear button |
| [scheduling.md](architecture/scheduling.md) | Automatic routine scheduling daemon with daily/hourly/interval schedules and timezone-aware DST; failed runs persist accumulated messages incl. the durable error message so the conversation shows why the routine stopped |
| [script-runner.md](architecture/script-runner.md) | Ephemeral Podman containers for executing workspace scripts and inline Python; TWO sandbox profiles backed by two separate images (restricted `quest-script-runner-<mode>`: no egress, socat bridge + QUEST_API_KEY confined to the loopback-only sandbox tool API server, where QUEST_API_KEY is an EPHEMERAL per-run sandbox token from the in-memory store in chat/sandbox_tokens.py -- `qsb_`-prefixed, minted by `sandbox_token_lease()` around each run_script/run_python container, TTL = clamped timeout + 15s grace, revoked on container exit, nothing persisted since containers die with the process; leases from inference API runs carry `block_mutating_tools` and the sandbox API 403s the mutating tools/routes for them; the sandbox app overrides every cookie/`users.api_key` auth dependency with `get_current_sandbox_user()` so the long-lived key is 401 on the sandbox port and a sandbox token is 401 on the main port, and the handlers no longer take an `api_key` argument so blank-key inference runs keep the bridge -- a second in-process uvicorn on `get_sandbox_port()` (QUEST_SANDBOX_PORT, default main port + 1) serving ONLY /api/tool-call, /api/authed-get|post, and /api/gmail-simple/* via the shared `register_sandbox_api_routes()` in chat/sandbox_api.py, so the container cannot reach the main server's HTTP surface; public `quest-script-runner-public-<mode>` for public-project conversations: internet egress, private-range iptables REJECTs, no credentials -- see public-projects.md), shared unit-tested argv builder `_build_script_podman_cmd`, both auto-built by run.py on Dockerfile/entrypoint changes; per-call startup cost trimmed by crun auto-detection passed as podman `--runtime` (chat/gemini_api/sandbox_runtime.py, `QUEST_SANDBOX_RUNTIME` override, logged at startup), a single atomic `iptables-restore` in both entrypoints, and a matplotlib font cache pre-built into both images (`MPLCONFIGDIR=/opt/matplotlib`); both profiles run under a no-symlink seccomp profile (symlink/symlinkat denied with EPERM -- the workspace mount is host-backed, so sandbox-created symlinks would redirect host-side file operations; generated into `<data_dir>/sandbox-seccomp.json` by chat/gemini_api/sandbox_seccomp.py from the host's default containers profile with the vendored repo-root script-runner-seccomp-fallback.json as fallback, fail-closed on generation failure) with a quest.py-lifespan scrub deleting any pre-fix workspace symlink (chat/workspace_symlinks.py) |
| [service-credentials.md](architecture/service-credentials.md) | Per-service upstream credential store (data/service_credentials/<service>.json, 0600, contents encrypted at rest as `{"encrypted": "qenc1:..."}` with plaintext files from the stdlib-only bootstrap/pre-baking swept into ciphertext at startup -- see encryption-at-rest.md) managed via the admin-only Settings > Service Credentials section; the admin surface is fully schema-driven: every service is a `ServiceSpec` in config/service_specs.py (label + CredentialField schema + is_configured predicate + optional flatten/unflatten/validate hooks for e.g. google_oauth's nested `web` shape; plugins pass a validate hook via QuestPlugin.credential_validate), the roster = CORE_SERVICES (google_oauth, ramp -- store-only, no legacy fallback -- smtp -- store-only outgoing email for the password sign-in flow's reset/sign-up/invite emails, read by auth/mailer.py -- and coingecko, a single-secret `api_key` card read store-first by load_coingecko_api_key() for the authed_get CoinGecko Pro registry entry, legacy `coingecko` server_credentials.json section migrated at startup) plus loaded plugins' credential services (register_plugin_credentials extends KNOWN_SERVICES; Slack arrives this way as service `slack` -- OAuth app client id/secret plus the optional shared bot/Socket Mode tokens the core Socket Mode worker consumes, from plugins/slack, legacy server_credentials.json slack section still migrating because migrate_legacy_credentials() runs in the lifespan after plugin load, loaders kept in auth/config.py -- GitHub as service `github` -- OAuth app client id/secret from plugins/github, same legacy-section migration, Microsoft 365 as service `m365` -- Entra ID tenant id + app client id/secret from plugins/m365, and Twitter/X as service `twitter` -- OAuth 2.0 app client id/secret from plugins/twitter, legacy standalone twitter_credentials.json migrating the same post-plugin-load way), and the endpoints are a schema+form-view list GET, single GET, and ONE generic PUT /admin/service-credentials/{service} (unknown keys rejected, required/required_if enforced, empty secret = keep stored) rendered by one generic FE card (GenericCredentialCard in ServiceCredentialsSection.tsx); startup migration from server_credentials.json (twitter from the standalone twitter_credentials.json) with the store winning when both exist; local mode pre-bakes store files from the parent-directory dev-config.json `service_credentials` mapping (existing files win) |
| [settings-data-connections.md](architecture/settings-data-connections.md) | Settings UI connector pattern, fully data-driven: the section lists ONLY the user's added (connected) services -- not-connected services live in an explicit add flow ("+ Add Connection" button -> inline picker of addable services -> oauth pick opens the popup, api_key pick advances to a key-entry step; no persisted added-but-not-connected state, `connected` = credential presence; groundwork for future multiple-connections-per-service) -- from GET /connectors, a LIST of rows covering every supported service (kind `oauth` with `connect_url`, or `api_key` with `key_url`/`key_field`/`disconnect_url` + key_hint; `available: false` hides a row from both list and picker -- now computed for ALL services needing admin-configured server credentials: core oauth rows probe their canonical loaders via `_server_configured`, plugins via `plugin_server_available`, Airtable exempt as user-supplied-PAT); the picker renders addable services as a grid of brand-icon tiles (serviceIcons.tsx `ServiceIcon` keyed by service id, generic plug fallback for unknown/new connectors) that DataConnectionsSection.tsx renders generically -- no per-service JSX, popup-blocked links reuse the row's connect_url; loaded plugins with a user connection get an auto-appended row backed by the user_service_credentials table -- api_key kind via the generic /auth/service-key/{service} key routes (custom placeholder via UserConnectionSpec.key_placeholder), oauth kind via the plugin's own mounted /auth/<id> router with the /auth/<id>?popup=1 connect-URL convention + optional needs_reauth hook (the GitHub plugin's row, granted-scope staleness badge) -- `available` = server-side configured (plugin_server_available), and connected_services gates plugin skills/tools on server-configured AND user-connected |
| [skill-library.md](architecture/skill-library.md) | Reusable skills (DB-backed user/shared/public/project) plus hardcoded system:* skills loadable on demand; three auto-load tiers (user / project / routine) merged and deduped into the system prompt; LLM tools list_skills/search_skills/load_skills (discover+load, incl. system:*) and the read-only inspection pair list_my_skills (categorized own/shared/public/project listing with per-skill autoload tiers, no bodies/system) + get_skill (single-skill full detail, owner-only shares roster, project-skill access fallback); skill WRITES (create_skill/edit_skill) ride on create_action_request (user-or-project target, visibility/email-sharing, clear_skill_shares on leave-shared, project-only project_autoload toggle, system:* non-editable; content edits are an edit_workspace_file-style old_string/new_string search-and-replace requiring the conversation to have loaded/read the skill, tracked in the skill_reads.json sidecar fed by auto-loads, composer loads, load_skills/get_skill, and create_skill), spec'd in the system:skill_management system skill |
| [slack-pending-request-notifier.md](architecture/slack-pending-request-notifier.md) | Background daemon (chat/slack_notifier.py, lifespan-started like the scheduler) that DMs users via the shared Slack bot token about open ("unanswered") action requests: cross-user grouped query `list_open_request_summaries()`, dual due-gates (oldest open request >= interval old AND >= interval since last DM, in-process `_last_notified` map), per-user `users.settings` keys `slack_pending_notifications_enabled` (absent = on, false = off) + `slack_pending_notification_interval_minutes` (default 60, clamped 5-1440, 0 clears on PUT /settings) edited in Settings > Slack (SlackSection.tsx checkbox + interval input); DM links to the new `/inbox` deep link (SPA route in quest.py + App.tsx URL->state sync for RequestsView, sidebar Inbox button navigates there) when `app_base_url` is set; core-only (no plugin import, no Socket Mode dependency), inert without Slack credentials or a per-user Slack connection |
| [slack-socket-mode.md](architecture/slack-socket-mode.md) | Slack-driven conversations (core machinery -- the Slack integration surface itself is the plugins/slack plugin): DM blocking reply tool backed by slack_reply wait handle, thread mapping, debounced reply resolution, user resolution via the user_service_credentials slack row's oauth_blob, read-only web UI with sidebar icon, native deep link, and per-user default model |
| [user-subagents.md](architecture/user-subagents.md) | Cross-user subagents with double approval, gated by the `user_subagents` conversation flag on the caller conversation AND the server-global admin feature gate (feature-gates.md, off by default; re-checked at proposal, same-turn validation, and approve time): `run_user_subagent` action request (caller approves the exact prompt; validation fails same-turn when the target user doesn't exist or can't see a listed skill) launches a read-only `origin="user_subagent"` conversation in the target's account driven headlessly by chat/user_subagent.py (USER_SUBAGENT_TOOLS = BASE_TOOLS + return_to_caller, no action requests / sub-agents, caller-chosen skills replace all autoload tiers); the subagent's `return_to_caller(response, files?)` shows the target a `subagent_return` card (full response + per-file preview via SubagentReturnFilesPreview/FileViewerModal); Approve copies files into the caller workspace under `.subagent_responses/` and resolves the caller's `user_subagent` wait handle (caller blocks via wait_for_handles), Revise re-drives the subagent with feedback, Deny ends the run immediately; run lifecycle on the `user_subagent_runs` table |
| [wait-handles.md](architecture/wait-handles.md) | Unified DB-backed wait handles for tools that block on user input; three kinds (slack_reply, action_request, user_subagent -- the last blocks a caller conversation on a cross-user subagent's approved return, see user-subagents.md) and three suspend sentinels (SuspendForWaitHandles, SuspendForSlackReply, SuspendForActionRequest); restart-resilient resume bucket closes dangling tool_uses with the row response (or a sibling regular tool's persisted chat_history.json tool_output replayed via `_load_persisted_tool_results()`; a `stopped` action_request row gets its response plus a note that the user's new message follows); `_run_resume` multi-card gate holds the continuation while any dangling action_request/slack_reply tool_use still has a pending row (parallel create_action_request batches) or a `stopped` row (Stop waits for the user's next send, never a headless wake-up); resumed continuation pushes to viewing tabs via the persistent WS; pending rows surfaced on `GET /conversations/{id}` and via `wait_handle_resolved` events to lock the FE composer across tabs/reloads |

## API

| File | Summary |
|------|---------|
| [chat-api.md](api/chat-api.md) | REST endpoints for conversations CRUD (the list endpoint supports server-side sidebar filters -- `exclude_projects`, `include_slack`, `include_inference`, NULL-origin rows always kept -- plus optional keyset paging via `limit`+`cursor` with `has_more`/`next_cursor` on every response; no params = legacy full list) plus the conversation tail endpoint, the composer-attachments upload (POST /conversations/{id}/composer-attachments for clipboard PNG/JPEG), and duplicate-workspace (POST /conversations/{id}/duplicate-workspace: new empty standalone conversation seeded with a copy of the source conversation's workspace files, surfaced as the sidebar "Duplicate Workspace" menu item); GET /conversations/{id} also returns pending_wait_handles so a fresh load can lock the composer when the suspended turn is waiting on an action request or slack_reply, plus the persisted `flags` array for the read-only composer label and the `expensive_resume` verdict (see expensive-resume.md) for the blocking cost-warning card; persistent multiplexed WebSocket (WS /app/api/stream) carries subscribe/catchup, send_message/stop (with image attachments plus an optional `attached_filenames` list for generic composer files pre-uploaded via POST /files/upload, and attachments-during-resume / user_not_found reject reasons, the latter from the fresh per-turn user DB re-read), durable and transient events, and per-user globals |
| [api-client-usage.md](api/api-client-usage.md) | TypeScript API client documentation for frontend consumption of backend REST endpoints |
| [admin-system-monitor-api.md](api/admin-system-monitor-api.md) | Admin-only System Reports endpoints under `/admin/system-monitor/`: `latest-active-conversations` (limit clamped 1..100 default 20, `include_routines=false` SQL-filters routine rows) and `most-expensive-conversations` (optional inclusive `start`/`end` ISO dates as UTC midnights, 400 on malformed/reversed, limit clamped 1..100 default 30; ranked by known per-model cost -- provider-reported OpenRouter amounts where captured, list-price estimates otherwise, each figure tagged `cost_source` reported/estimated/mixed incl. the user-report splits' `*_source` companions -- over in-range calls with token tiebreak, deleted conversations kept with "(deleted conversation)" title + owner from call-row user_id) plus `user-report` (same optional `start`/`end` semantics via the shared `_resolve_range_window`; one row per user sorted by known in-range cost: range-clipped non-routine active-day union from chat_history.json, non-routine/routine in-range conversation counts split on surviving rows' `routine_id` -- deleted-conversation calls count as non-routine -- per-split cost figures each nulling independently when a model in that split is unpriced, the routine split itemized per routine in `routine_costs` (routine + project name, run count, per-routine null-on-unpriced cost, priciest first), and all-conversation per-model `usage_by_model`/`usage_total` aggregates from `get_usage_by_user()`) and the parameter-less `guides-report` (every user guide incl. empty defaults with owner + `routine_count` of routines still referencing it, plus every project with non-empty `projects.guide` instructions, `kind`-discriminated rows sharing one key set, `content_length` instead of text, sorted by owner email); the conversation endpoints share one row shape via `_admin_conversation_view`: title via `ChatStorage._resolve_list_title`, `routine_id`, `usage_by_model` provider-discriminated union rows + coarse `usage_total` (cost null when any model unpriced) from one batched GROUP BY per provider raw table with per-call >200K long-context tier grouping, plus `latest_context_tokens` (latest top-level call's input-side size, batched) and `active_days` (distinct UTC user-message days, whole lifetime); admin-gated via inline `is_admin()` (403 while impersonating) |
| [airtable-api.md](api/airtable-api.md) | Airtable read-only access via authed_get with PAT auth and allowed_endpoints validation |
| [calendar-api.md](api/calendar-api.md) | Google Calendar access via authed_get (reads) and two action requests for writes: create_calendar_invite (new events) and edit_calendar_event (PATCH an existing event with read-before-write enforcement via the model-supplied `expected_updated` timestamp checked against the live event at proposal AND approve time, live `current_event` snapshot injected for the before/after card incl. a description line diff and recurrence scope) |
| [docs-api.md](api/docs-api.md) | Google Docs access via authed_get (document reads) and Drive API (document listing), plus the `google_export_doc` tool_call tool that exports a native Doc into the workspace in any Docs export format (pdf/docx/odt/rtf/txt/md/html/epub/zip via Drive `files.export`; Docs-only mimeType pre-check with cross-pointer to download_drive_file, title-derived default filename, `/export` GET allow-listed but blocked in authed_get without output_file like alt=media) |
| [drive-api.md](api/drive-api.md) | Google Drive access via authed_get, download_drive_file tool, Save to Drive (markdown->Google Doc), and the agent-driven upload_to_drive + create_drive_folder action requests (multi-file raw-bytes workspace-file upload -- `files` list, up to 10/request, batched-into-one-approval guidance in the model docs -- with optional on-the-fly destination-folder creation, Drive folder creation, 50 MB/file cap, drive.file scope, shared-drive support; shared plumbing in chat/action_request_types/_drive.py) |
| [federal-register-api.md](api/federal-register-api.md) | Federal Register read access via authed_get (documents, agencies, public-inspection docs); no-auth service (None loader, no-op injector, requires_user unset), path-scoped to /api/v1 with allowed_endpoints regexes; surfaced via the ungated system:federal_register skill |
| [sec-edgar-api.md](api/sec-edgar-api.md) | SEC EDGAR read access via authed_get (data.sec.gov submissions + XBRL companyfacts/companyconcept/frames, plus the two www.sec.gov ticker->CIK map files); no-auth service (None loader, no-op injector, requires_user unset), plain-hostname keys gated by allowed_endpoints regexes with a SEC User-Agent default header; 10-digit zero-padded CIK; surfaced via the ungated system:sec_edgar skill |
| [file-browser-api.md](api/file-browser-api.md) | File management endpoints for conversation workspaces: list (returns all entries incl. dotfiles; FE hides them client-side), upload (POST /files/upload, also reused by the composer "Attach" button for generic pre-send file attachments -- no new endpoint), download (also the fetch path for inline image/PDF preview and the `src` target for inline markdown workspace images in chat; raster image extensions served with real MIME + `Content-Disposition: inline` via `_INLINE_IMAGE_MIMES`, SVG deliberately excluded), download folder as zip, delete, create folder, save to Drive; `.md` files render as HTML and `.csv` files render as a client-side sortable table in FileViewerModal (react-markdown with "Show rendered" toggle; utils/csv.ts parser with "Show raw" toggle) off the existing raw-text content fetch -- no backend change beyond adding `.csv` to VIEWABLE_EXTENSIONS; symlinks are banned from workspaces (the sandbox seccomp profile blocks creating them -- see script-runner.md) with defense-in-depth guards here: uploads open the leaf O_NOFOLLOW (existing-symlink leaf rejected as ValueError), folder zips refuse symlink descendants, workspace duplication/moves skip or delete symlinks (chat/storage.py); the list/preview/zip/info/delete routes run their sync file_storage helpers via `asyncio.to_thread` so an unbounded workspace tree walk (`rglob` counts, zip) never stalls the event loop; non-regular files (sandbox-created FIFOs) are rejected before any blocking read -- `get_file_content` pre-checks `is_file()` then re-checks `S_ISREG` on a non-blocking `os.open` descriptor, `create_folder_zip` refuses folders with special-file descendants |
| [gcp-api.md](api/gcp-api.md) | Read-only Google Cloud (GCP) access over the existing per-user Google OAuth: projects/orgs (Resource Manager), Compute Engine VMs/disks (incl. aggregated + orphaned-disk scans, plus per-instance IAM policy via the slash-style instances/{id}/getIamPolicy GET), GKE clusters, Cloud Logging -- GETs via authed_get, the POST-only reads (organizations:search, entries:list, and the project-level IAM read projects/{id}:getIamPolicy v1+v3 -- POST-but-read-only, body carries only GetPolicyOptions) via the authed_post sibling; read-only is app-enforced by the GET/POST allow-lists ([^/:]+ id segments block :-write verbs incl. :setIamPolicy), the OAuth scope is full cloud-platform (Compute/GKE reject read-only variants) so re-consent is required, and the Google callback now stores granted-not-requested scopes; the ONE write is the approval-gated `reset_gcp_instance` action request (hard power-cycle a stuck VM via Compute instances.reset, outside the authed_get/authed_post allow-lists: proposal-time instance fetch rejects bad ids / powered-off TERMINATED/SUSPENDED VMs same-turn and injects live status + machine type into the card, execute pre-checks the cloud-platform scope); surfaced via gated system:gcp skill |
| [gmail-api.md](api/gmail-api.md) | Gmail Simple access as five dedicated dynamic tools (all markdown->HTML email bodies -- drafts, self-sends, and the M365 tools reusing the helper -- pass through the allow-list `sanitize_email_html()` inside `convert_markdown_to_html()`: no remote references, `<img>` only with a `cid:` attachment source and remote images degrade to alt text; draft attachments on both Gmail and M365 get a filename-derived `Content-ID` (`attachment_content_id()`, image types inline) reported as `attachments[].content_id` in the result so `![caption](cid:<filename>)` embeds an attached image, `style`/`<style>`/`<script>`/comments dropped, links http/https/mailto only -- so a prompt-injected image URL can't exfiltrate the model-visible API key via the mail client's image fetch) (get_gmail_messages markdown-rendered 1-50-id fetch, list_gmail_labels, get_gmail_message_urls, create_gmail_draft, send_gmail_to_self -- the self-send takes the same workspace/drive/gmail `attachments` list as drafts via shared `_resolve_request_attachments()`/`_add_attachments_to_message()` helpers, so it can attach files and inline `cid:` images without an approval card; handlers wrap the api/gmail endpoint functions with conversation_id from dispatch context, HTTPException -> `{"error": ...}` conversion via _run_gmail_simple_endpoint; the `/api/gmail-simple/*` HTTP routes stay registered so run_script/run_python sandbox code can call them over the local proxy with QUEST_PORT/QUEST_API_KEY -- script calls have no conversation context, so URLs stay inline and workspace draft attachments are tool-only) and Raw API via authed_get; Quest-managed label tools list_gmail_quest_labels / modify_gmail_labels (user-configured `gmail_labels` setting from Settings > Gmail, each name a nested `[Quest]/<name>` label, add/remove on up to 100 message ids per call via one batchModify, add-labels created on demand, validation shared in api/gmail/quest_labels.py; archive_gmail_message also takes optional add_labels to archive-and-label in one call) |
| [inference-api.md](api/inference-api.md) | One-shot non-streaming inference endpoint for internal applications: POST /api/inference runs a single prompt headlessly as the authenticated user and returns the final markdown; authenticated ONLY by named per-user bearer tokens from Settings > Inference API (`inference_api_keys` table, sha256-hashed with `qst_` prefix + display hint, raw token shown once at creation; CRUD under /app/api/inference-api-keys); each call creates a read-only `origin="inference_api"` conversation driven like a user_subagent run (chat/inference_api.py), hidden from the sidebar by default like Slack conversations (client-side origin filter + "Show Inference API Runs" toggle + zap icon in Sidebar.tsx; not convertible to a project) with INFERENCE_API_TOOLS = BASE_TOOLS + return_final_response (no action requests, no sub-agents; inference runs must not change anything outside their workspace, so the approval-free mutating tools -- `"mutating": True` on core `TOOL_CALL_REGISTRY` specs / `PluginTool.mutating`, collected by `mutating_tool_call_tools()`: the four Gmail write tools plus the plugin self-DM/self-SMS/Outlook mailbox tools -- and the `MUTATING_PROXY_PATHS` internal routes are hard-rejected at dispatch via `is_inference_api`, refused by the sandbox tool API for the run's `block_mutating_tools` sandbox lease, and hidden from the prompt), a no-intermediate-output system prompt (custom prompt/guides excluded, user autoload skills included), response captured from the return_final_response tool_use via the FinishInferenceResponse sentinel, one nudge if the model ends without returning, 15-min timeout, a curl_proxy recursion block in route_dispatch, and the run driven with a blank `api_key` so the owner's reusable `users.api_key` never reaches the prompt/skill docs (sandbox scripts keep the tool-API bridge: their QUEST_API_KEY is the per-run ephemeral sandbox token minted from the user id, see script-runner.md) |
| [project-db-api.md](api/project-db-api.md) | REST endpoints for browsing and deleting project SQLite database tables with pagination and server-side column sorting |
| [ramp-api.md](api/ramp-api.md) | Ramp spend-management read access via authed_get (transactions, cards, bills, reimbursements, vendors, statements, accounting, audit logs) over per-user OAuth (all read scopes + offline_access; cards:read_vault excluded); rotating refresh tokens handled by proactive loader refresh + retry_on_401; resource-root allow-list keeps card-vault/token/webhook paths and all POSTs unreachable; connector hidden until an admin configures client credentials (`available: false`); surfaced via gated system:ramp skill |
| [projects-api.md](api/projects-api.md) | REST endpoints for managing projects that group conversations with shared workspace and guide, incl. POST /projects/from-conversation (create a project seeded from a standalone conversation: moves workspace files, sets `conversations.project_id`, rejects Slack/already-in-project conversations, no `public` field) and the creation-only `public` flag on POST /projects (public projects 400 routine creation and project-skill writes; see public-projects.md) |
| [routines-api.md](api/routines-api.md) | REST endpoints for managing routines (canned prompts attached to projects) plus GET/PUT for per-routine skill auto-load toggles; a `guide_id` on create/update 403s `guides_disabled` unless the `guides` feature gate is open for the user (clear_guide always accepted); PUT accepts an `expected_updated_at` token and returns flat 409 stale_update with the current row on optimistic-concurrency mismatch |
| [schedules-api.md](api/schedules-api.md) | REST endpoints for managing routine schedules for automatic execution on timers; PUT accepts an `expected_updated_at` token and returns flat 409 stale_update with the current row on optimistic-concurrency mismatch |
| [sheets-api.md](api/sheets-api.md) | Google Sheets access via authed_get (spreadsheet reads) and Drive API (spreadsheet listing); cell writes via the edit_google_spreadsheet action request (bounded A1 rectangle + 2d values, read-before-write current_values verified against the live sheet at proposal AND approve time (formula cells must be claimed by formula text via a FORMULA-rendering read, not computed values), new `spreadsheets` write scope with execute-time reauth pre-check, diff-table approval card with 2 rows/cols of context) |
| [skills-api.md](api/skills-api.md) | REST endpoints for Skill Library CRUD, sharing, auto-load, search, and user search; plus a pointer to the read-only agent tools (list_skills/search_skills/load_skills + list_my_skills/get_skill) that read the same skill data and the agent write path (create_skill/edit_skill action requests) that mutates it |
| [slides-api.md](api/slides-api.md) | Read-only Google Slides access via authed_get (per-user Google OAuth, new `presentations.readonly` scope requiring re-consent; get presentation + single page, write verbs blocked) and Drive API mimeType listing; surfaced via gated `system:slides` skill; large decks need `fields=`/`output_file` |
| [tasks-api.md](api/tasks-api.md) | Google Tasks access via authed_get for read-only task lists and tasks |

## Setup

| File | Summary |
|------|---------|
| [development.md](setup/development.md) | Setup instructions for Quest development environment on a local machine; run.py --local/--staging/--prod launcher, QUEST_DATA_DIR, server_config.example.json schema, shared parent-directory dev-config.json (vertex_service_account + service_credentials + oauth_hostname + allowed_login_domain pre-baking in local mode) |
| [development-workflows.md](setup/development-workflows.md) | Development workflow: PR-based branching (no direct commits to main; humans merge), releases (annotated `v<semver>` git tags cut by `scripts/tag_release.py` with clean-main/ordering guards; the `v*` namespace is reserved and immutable under the GitHub "release tags" ruleset checked in as `.github/rulesets/release-tags.json`, GitHub Release published by `.github/workflows/release.yml`, version resolved from the nearest tag at startup by `config/version.py` and surfaced on `GET /app/api/version`, the run.py banner and Settings > About), syntax checking, pytest tests (testpaths = tests/ + plugins/ -- core suite in tests/, each plugin's suite in plugins/<name>/tests with a plugin_fixture-based conftest, out-of-tree QUEST_PLUGIN_PATH plugin suites auto-collected by the repo-root conftest.py on a bare `uv run pytest`), code validation |
| [production.md](setup/production.md) | Production deployment guide for the Quest application, incl. the run.py --prod first-run bootstrap wizard (scripts/bootstrap_prod.py: sign-in method -- email/password by default, Google for installs that already have Google OAuth -- admin_emails, allowed_login_domain (public mailbox domains never suggested; admins outside it auto-allow-listed), the app_base_url public URL, then admin passwords (scrypt hashes into pending_admin_passwords.json, applied by the server at startup) or the Google OAuth store file, Vertex service-account key auto-exported as ADC; gating requires an admin password for password sign-in, detected via stdlib sqlite3) and the Serving over HTTPS section (most OAuth providers reject non-HTTPS callback URLs, so set `app_base_url` to an https URL fronted by Tailscale Serve, a Cloudflare Tunnel behind Zero Trust Access, or a reverse proxy) |
| [ramp-setup.md](setup/ramp-setup.md) | Creating and configuring a Ramp OAuth app for Quest integration (all read scopes, redirect URI, admin credential entry) |

## Plugins

Plugin-specific docs live inside each plugin's directory under `plugins/<dir>/docs/`.

| File | Summary |
|------|---------|
| [github-api.md](../plugins/github/docs/github-api.md) | GitHub read access, packaged as the in-tree plugins/github plugin (id `github`; the oauth-kind reference plugin -- see plugins.md): authed_get service entry (repos, issues, PRs, commits, contents, search, Actions runs/jobs/workflows) plus the github_get_job_log plugin tool (renamed from get_github_job_log for the <id>_ prefix rule) for two-hop signed-URL log downloads; plugin-provided /auth/github OAuth router (same URLs as pre-plugin), token JSON incl. granted scopes in user_service_credentials.oauth_blob, needs_reauth = granted scopes no longer covering GITHUB_SCOPES (skipped for scope-less GitHub App `ghu_` tokens), connector row hidden until an admin configures the OAuth app client credentials |
| [github-app-setup.md](../plugins/github/docs/github-app-setup.md) | Creating and configuring a GitHub OAuth App for Quest integration (served by the plugins/github plugin; admin client id/secret in the github credential store entry) |
| [m365-mail-api.md](../plugins/m365/docs/m365-mail-api.md) | Outlook (Microsoft 365) email access over Microsoft Graph, packaged as the in-tree plugins/m365 plugin (id `m365`; oauth kind with EXPIRING tokens -- proactive refresh within 5 min of expiry under a per-user lock, rotated refresh token persisted, retry_on_401 backstop, granted-scope needs_reauth badge): seven `m365_*` tools mirroring the Gmail surface (m365_get_mail_messages markdown-rendered 1-50-id fetch with shared markdownify + `(#N#)` URL-replacement cache, m365_list_mail_folders, m365_get_mail_message_urls, m365_create_mail_draft -- new/reply/forward via Graph createReply/createForward with workspace + outlook fileAttachment attachments, <=3MB simple POST else chunked upload session, 25MB total, composed body REPLACES the auto-quote --, m365_send_mail_to_self to the OAuth-captured mailbox address ([Quest] prefix, markdown->HTML through the shared no-remote-references `sanitize_email_html()` -- see gmail-api.md), m365_archive_mail_message ('Quest archived' category + move to archive, id changes on move), m365_save_mail_attachment workspace download via $value with 50MB cap) plus raw read-only Graph search/list via authed_get (graph.microsoft.com GET-only allow-list: /me, messages, mailFolders, attachments; $search/$filter guidance in the gated system:m365 skill); script bridge exposes the two read tools; deliberate gaps vs Gmail: no Quest-managed label tools (needs a settings UI), no Drive attachments, no batch endpoint |
| [office365-setup.md](../plugins/m365/docs/office365-setup.md) | Creating and configuring a Microsoft Entra ID app registration for the Quest Office 365 email integration (delegated Mail.ReadWrite/Mail.Send/User.Read/offline_access scopes, web redirect URI /auth/m365/callback, tenant+client id+secret in the m365 credential store entry served by plugins/m365) |
| [slack-api.md](../plugins/slack/docs/slack-api.md) | Slack access, packaged as the in-tree plugins/slack plugin (id `slack`, oauth kind with non-expiring tokens in user_service_credentials.oauth_blob -- migration `f3a9c5d81b42` moved users.slack_oauth; see plugins.md): the nine grandfathered-name tool_call tools (reads incl. find_slack_channel, plus the no-approval send_slack_dm_to_self self-DM kept in the public-project allowlist via the core-owned exemption), the grandfathered send_slack_message/send_slack_dm action requests with enrich_params_for_preview channel-name/thread-context resolution, the system:slack skill, the /auth/slack OAuth router (same pre-plugin URLs + new /auth/slack/disconnect), the admin credential card (client id/secret + optional shared bot/Socket Mode tokens read by the core Socket Mode worker), and full script-bridge access; the Slack-driven runtime stays core (slack-socket-mode.md) |
| [slack-app-setup.md](../plugins/slack/docs/slack-app-setup.md) | Creating and configuring a Slack app for Quest integration (served by the plugins/slack plugin; admin credentials in the slack credential store entry) |
| [twitter-api.md](../plugins/twitter/docs/twitter-api.md) | Twitter/X access, packaged as the in-tree plugins/twitter plugin (id `twitter`; see plugins.md): read-only authed_get allow-list on api.twitter.com (profile/user lookup, DM events, DM conversation metadata, bookmarks, tweet lookup -- upstream query params pass through verbatim, old /api/twitter/* proxy routes gone and blocked in route dispatch), per-user OAuth 2.0 PKCE tokens in user_service_credentials.oauth_blob (migration e2f8a4c61b93 moved users.twitter_oauth; ~2h expiry, rotating refresh tokens, proactive refresh under a per-user lock + retry_on_401 backstop, bookmark.read staleness drives the needs_reauth badge), DM sending via the grandfathered `send_twitter_dm` action request (handler + recipient-name enrich hook in plugins/twitter/handlers.py), surfaced via gated system:twitter skill |
| [twitter-x-setup.md](../plugins/twitter/docs/twitter-x-setup.md) | Creating and configuring Twitter/X OAuth 2.0 App with PKCE flow (admin client id/secret in the plugin-registered twitter credential store entry served by plugins/twitter) |
| [twilio-api.md](../plugins/twilio/docs/twilio-api.md) | Twilio SMS, packaged as the in-tree plugins/twilio plugin (id `twilio`; see plugins.md): NO OAuth -- the oauth-kind connection's `/auth/twilio` router is a phone-number verification flow (user types their number, a 6-digit code is texted via the Messages API, the salted code hash + expiry + attempt cap live in the credential row's `oauth_blob.pending`, the verified number in `phone_number`; popup page is plugin-served HTML posting the shared `oauth_callback_success`); admin card = Account SID / Auth Token / sending number-or-`MG` SID + a **trusted channel** bool re-read per call; `twilio_send_self_sms` (no approval, recipient fixed to the verified number) sends free-form `body` only when trusted, otherwise only a `template` name from the user's pre-written messages sent verbatim (`users.settings.twilio_sms_templates`, edited in the core Settings > SMS Messages section via the plugin's `GET/PUT /auth/twilio/templates`, nav item shown only while the `twilio` connector row is available); `twilio_list_sms_templates` reports the mode + list; texts to anyone else always ride on the approval-gated `twilio_send_sms` action request (E.164 `to`, 1600-char body); no script-bridge access, no authed_get entry |
| [twilio-setup.md](../plugins/twilio/docs/twilio-setup.md) | Configuring Twilio for Quest: Account SID/Auth Token, an SMS-capable number or Messaging Service SID, the admin trusted-channel switch semantics, and the per-user phone verification + SMS Messages flow |
| [unifi-api.md](../plugins/unifi/docs/unifi-api.md) | UniFi Network + Protect, packaged as the in-tree plugins/unifi plugin (id `unifi`; see plugins.md): read-only, DIRECT access to a UniFi OS console on the LAN (never unifi.ui.com) over the official local Integration APIs (`/proxy/network/integration/v1`, `/proxy/protect/integration/v1`, `X-API-KEY` header); admin card = controller `host` (normalized to an https:// origin) + `verify_tls` bool (off by default, consoles are self-signed); per-user connection is oauth-kind WITHOUT OAuth -- UniFi issues a separate key per application, so the `/auth/unifi` popup collects a Network and/or Protect API key, each verified live against its application before landing in `oauth_blob` (`POST /auth/unifi/keys`: omitted = keep, `""` = remove, at least one must remain; `/auth/unifi/disconnect`); eleven `unifi_*` tools: controller info, sites, `unifi_get_network_status` (overall ok/degraded/down from the legacy `stat/health` WAN/WWW subsystems when the console accepts the key there, else from gateway state; gateway uplink stats; device counts + not-online list; client counts), devices list/detail (+ `statistics/latest`), connected clients list (all pages collected, client-side type/search filter + paging) / detail, Protect cameras list/detail, `unifi_get_camera_snapshot` (live JPEG saved to `unifi-snapshots/` and attached to the tool result as an image; refuses DISCONNECTED cameras), other Protect device families; no action requests, no authed_get entry; reads in the script bridge, snapshot tool-only |
| [unifi-setup.md](../plugins/unifi/docs/unifi-setup.md) | Connecting Quest to a UniFi OS console: admin controller host + TLS switch, per-user Network / Protect API keys created under each app's Settings > Control Plane > Integrations, version requirements, troubleshooting |
| [iru-api.md](../plugins/iru/docs/iru-api.md) | Iru (formerly Kandji) Apple/MDM read access, packaged as the in-tree plugins/iru plugin (id `iru`; see plugins.md): admin card = the tenant API origin `api_url` (normalized by `normalize_api_url`, must be a `<sub>.api.iru.com` / `.api.eu.iru.com` / legacy `.api.kandji.io` host -- runtime-configured, so everything is `tools`, no authed_get entry); per-user connection is the generic `api_key` kind (each user's own tenant-level bearer token with Iru-side per-endpoint permissions, shape-checked by `validate_api_token`, 403 = missing permission surfaced as `iru_auth_failed`); fifteen read-only `iru_*` tools: tenant info/licensing, devices list (compact `device_row`s, blueprint-name resolution, limit/offset) and `iru_get_device` (id / serial / name resolution with ambiguity candidates + selectable sections: details, status, library_items, apps, activity, commands, parameters, lost_mode), blueprints list/get (+ assigned library items), per-device library item status (client-side status filter over collected pages), users (cursor-paged) / get user + devices, tags, `iru_prism_query` (any Prism category, JSON filter passthrough, `fields` projection, unfiltered `prism/count`), threats, vulnerabilities list/get, audit events (cursor), ADE devices; 429 -> `iru_rate_limited` + retry_after; device secrets endpoints deliberately unwrapped; NO action requests yet (write side planned); all tools in the script bridge |
| [iru-setup.md](../plugins/iru/docs/iru-setup.md) | Connecting Quest to an Iru (Kandji) tenant: admin tenant API URL (API host, not the web-app host; US/EU/legacy kandji.io forms), per-user API token created under Settings > Access > API Token with read-only permissions (no secrets / actions), troubleshooting 401 vs 403 vs 429 |
| [telegram-api.md](../plugins/telegram/docs/telegram-api.md) | Telegram, packaged as the in-tree plugins/telegram plugin (id `telegram`; see plugins.md): NO OAuth -- the oauth-kind connection's `/auth/telegram` router runs Telegram's own login via Telethon (`send-code` -> `verify` -> optional `2fa` JSON endpoints behind a plugin-served popup page; the in-flight login -- phone, `phone_code_hash`, pre-auth session string, stage, expiry, attempt cap -- lives server-side in the credential row's `oauth_blob.pending`, the authorized `StringSession` in `oauth_blob.session`, an existing session survives a failed re-login, `disconnect` best-effort logs out); admin card = MTProto app `api_id`/`api_hash` (legacy `telegram_app_info` section of server_config.json migrated + int api_id normalized by the `post_load` hook); `TelegramClientManager` keeps one connected Telethon client per user (`telegram_not_connected` / `telegram_session_expired` structured errors, dropped on reconnect/disconnect, all closed by the plugin's `on_shutdown` hook); four `telegram_*` read tools (`telegram_get_me`, `telegram_list_dialogs`, `telegram_get_messages`, `telegram_list_contacts` -- `requires_service: telegram`, script-bridge enabled, NOT public-project allowlisted, `/api/telegram` prefix still blocked in route dispatch) plus the grandfathered `send_telegram_message` action request (`unprefixed_action_types`; dialog-name preview via the handler's `enrich_params_for_preview` hook); migration `d7a1f3c9e2b4` moved `users.telegram_session` into the rows |
| [telegram-setup.md](../plugins/telegram/docs/telegram-setup.md) | Configuring Telegram for Quest: the MTProto app `api_id`/`api_hash` from my.telegram.org in the admin Service Credentials card (legacy server_config.json `telegram_app_info` fallback migrated at load), the per-user phone/code/2FA login popup, session storage, and troubleshooting |

## Root

| File | Summary |
|------|---------|
| [README.md](README.md) | Comprehensive Quest documentation table of contents covering architecture, setup, and API reference |
| [STYLE_GUIDE.md](STYLE_GUIDE.md) | Documentation style conventions: reference-based indexing, topic organization, no code duplication |
