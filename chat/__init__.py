"""Chat app modules."""

# Logging for the "chat" namespace is configured by the unified
# logging config in chat/logging_config.py, which is applied either
# via uvicorn's log_config parameter or by calling configure_logging()
# directly.  No per-module handler setup is needed here.
